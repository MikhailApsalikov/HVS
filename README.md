# Hunters versus Spiders

Браузерная игра о защите замка девятью лучниками. Три сложности, шесть типов пауков, восемь способностей, восемнадцать талантов в трёх ветках, основные характеристики и броня, полный каталог предметов, графика и звук.

## Запуск

Требуются Node.js 24+ и pnpm 11.19.0 (версия закреплена в `package.json`).

```sh
pnpm install --frozen-lockfile
pnpm dev
```

Клавиши 1–9 стреляют по соответствующим линиям. Q–I активируют способности; физические клавиши работают и при русской раскладке. Покупка и продажа предметов — двойным нажатием между уровнями.

## Проверки

```sh
pnpm exec playwright install chromium
pnpm check
```

Одна команда проверяет типы, форматирование, правила, регрессии, покрытие, производственную сборку и браузерные сценарии. Эти же проверки запускаются в GitHub Actions на push и pull request. Для локального установленного Chrome можно задать `PLAYWRIGHT_CHANNEL=chrome`.

Отдельно доступны `pnpm test`, `pnpm test:watch`, `pnpm test:coverage`, `pnpm test:e2e`, `pnpm build` и `pnpm preview`. Покрытие сохраняется в `coverage/index.html`; следы упавших браузерных сценариев — в `test-results`.

## Правила и устройство

- [Действующие правила чисел и изменения поведения](docs/rules.md)
- [Архитектура и места для дальнейших изменений](docs/architecture.md)
- [Тестирование и карта сохранённых возможностей](docs/testing.md)

Исторические `.txt` в `documentation/` описывают старую версию. При расхождении действуют новые правила из `docs/rules.md` и исполняемые определения в `src/content` и `src/domain/rules`.

Сохранения хранятся локально в браузере. Новый формат сохраняет состояние поля и способностей; старые форматы 1/2 читаются как контрольные точки. Публикация игры не требуется для локального запуска.
